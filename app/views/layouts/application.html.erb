<!DOCTYPE html>
<html>
<head>
  <title>Pausm: it's really awesome</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
	<script>
		$(function() {
		    $(window).scroll(function(){
		        var scrollTop = $(window).scrollTop();
		        if(scrollTop != 0)
		            $('.navbar-fixed-top').stop().animate({'opacity':'0.05'},200);
		        else   
		            $('.navbar-fixed-top').stop().animate({'opacity':'1'},200);
		    });
		     
		    $('.navbar-fixed-top').hover(
		        function (e) {
		            var scrollTop = $(window).scrollTop();
		            if(scrollTop != 0){
		                $('.navbar-fixed-top').stop().animate({'opacity':'1'},200);
		            }
		        },
		        function (e) {
		            var scrollTop = $(window).scrollTop();
		            if(scrollTop != 0){
		                $('.navbar-fixed-top').stop().animate({'opacity':'0.05'},200);
		            }
		        }
		    );
		});
	</script>
</head>
<body>

	<%= render "layouts/navbar" %>

	<div class="ask_question"><%= link_to "Ask Question", new_question_path, class: "ask_question" %> </div>

	<div id="container">
		
		<%= yield %> <!-- grabs templates and other info from elsewhere -->

	</div>

</body>
</html>
